# Base requirements for all environments
-r requirements_web.txt

# To run example notebooks, install required and test dependencies
.[test]

# Ensure Plotly is explicitly installed in environments that ignore -r includes
plotly>=5.17.0
